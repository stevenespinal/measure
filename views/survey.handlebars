
<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="nav-menu-container">
        <div class="container">
          <a class="navbar-brand js-scroll-trigger" href="/">MEASURE</a>
          <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fa fa-bars"></i>
          </button>
          <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto nav-menu">
              <li class="nav-item">
                <a class="nav-link js-scroll-trigger menu-active" href="/sign-in">Sign In</a>
              </li>
              <li class="nav-item">
                <a class="nav-link js-scroll-trigger" href="/about">About</a>
              </li>
              <li class="nav-item">
                <a class="nav-link js-scroll-trigger" href="/contact">Contact</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>

<div class="container survey">
    <div class="row">
        <div class="col-md-12">
            <div class="inline-modal-errors new-error">
                <p class="paragraph">Please take a few moments to answer the below questions so that we can match you up with a mentor that could best assist you.</p>
            </div>
        </div>
        <div class="col-md-12">
           <div class="grid">
        <div id="quiz">
            <h1 class="text-center" style="margin-top: 30px;"><em>Connect with a mentor today!</em></h1>
            <hr style="margin-bottom: 20px">
 
            {{!-- <p id="question"></p> --}}

             <div id="surveyElement"></div>
            <div id="surveyResult"></div>

                    <hr style="margin-top: 50px">
           
        </div>
    </div>
        </div>
    </div>
</div>

<script type="text/javascript">

        
Survey
.StylesManager
.applyTheme("default");
{{!-- .applyTheme("bootstrap"); --}}

var json = {
title: "Mentor Match",
showProgressBar: "bottom",
showQuestionNumbers: "off",
firstPageIsStarted: true,
startSurveyText: "Start Quiz",
pages: [
    {
        questions: [
            {
                type: "html",
                html: "Start the quiz to find your mentor."
            }
        ]
    }, {
        questions: [
            {
                type: "checkbox",
                name: "question1",
                title: "What areas of your life do you need guidance with?",
                isRequired: true,
                choices: [
                    "Finances", "Leadership", "Mental Health", "Lifestyle"
                ],
            }
        ]
    }, {
        questions: [
            {
                type: "checkbox",
                name: "question2",
                title: "What areas would you like to work on?",
                isRequired: true,
                choicesOrder: "random",
                choices: [
                    "Mindfullness Skills", "Interpersonal Effectiveness Skills", "Emotion Regulation Skills", "Distress Tolerance Skills",
                ],
            }
        ]
    }, {
        questions: [
            {
                type: "checkbox",
                name: "question3",
                title: "What goals do you hope to achieve?",
                isRequired: true,
                choicesOrder: "random",
                choices: [
                "Stabalizing your thoughts", "Practice good coping skills", "Enhance self-confidence and self-love", "Experience reality as it is"
                ],
            }
        ]
    }, {
    questions: [
    {
          type: "checkbox",
          name: "question4",
          title: "Which gender would you prefer your mentor to be?",
          isRequired: true,
          choicesOrder: "random",
                choices: [
                "Male", "Female", "Non-binary", "No preference"
                ],
                }
            ]
        }
    ],
    completedHtml: "<h4>Meet your matches</h4>"
};


{{!-- completedHtml: "<h4>Meet your matches</h4>" --}}
{{!-- completedHtml: "<h4>Meet your matches</h4><script id=setmore_script type=text/javascript src=https://my.setmore.com/webapp/js/src/others/setmore_iframe.js></script><a id=Setmore_button_iframe style=float:none href=https://measure.setmore.com/bookingpage/7dd80068-e7d1-448c-a2f6-4d4fbbb9bb48><img border=none src=https://my.setmore.com/webapp/images/bookappt/SetMore-book-button.png alt=Book an appointment with Measure using SetMore /></a>"
     --}}


window.survey = new Survey.Model(json);


survey
.onComplete
.add(function (result) {
    document
        .querySelector('#surveyResult')
        .textContent = "Result JSON:\n" + JSON.stringify(result.data, null, 3);
});


$("#surveyElement").Survey({model: survey});;


    $('#submit').on('click', function(event) {
        event.preventDefault();
        // Form validation
        var valid = true;

        if ($("#question1").val() === "empty" || $("#question2").val() === "empty" || $("#question3").val() === "empty" || $("#question4").val() === "empty") {
            valid = false;
        }
        else if (valid === true) {
            var newUser = {
                scores: [
                    $("#question1").val(),
                    $("#question2").val(),
                    $("#question3").val(),
                    $("#question4").val()
                
                ]
            };
            var currentURL = window.location.origin;
            // Ajax call for receiving response after POST req
            $.post(currentURL + "/api/mentors", newUser,
                function(data) {
                    if (data) {

                        $("#bffName").text(data.name);
                        $("#bffImg").attr("src", data.photo);

                        // Show modal
                        $("#executeModal").modal("toggle");

                        // Clear after submission
                       
                        $("#question1").val(""),
                        $("#question2").val(""),
                        $("#question3").val(""),
                        $("#question4").val("")
                         

                    } else
                    // If a required field is missing, show alert
                        alert("Survey is incomplete!");

                });
        };
    })

        </script>
 
            {{!-- <div class="buttons">
                <ul id="btn0" class="butn btn-warning" style="padding: 20px; width: auto;"><li id="choice0"></li></ul>
                <ul id="btn1"  class="butn btn-info" style="padding: 20px; width: auto;"><li id="choice1"></li></ul>
                <ul id="btn2"  class="butn btn-danger" style="padding: 20px; width: auto;"><li id="choice2"></li></ul>
            </div> --}}
 


{{!-- <script id="setmore_script" type="text/javascript" src="https://my.setmore.com/webapp/js/src/others/setmore_iframe.js"></script>
<a id="Setmore_button_iframe" style="float:none" href="https://measure.setmore.com/bookingpage/7dd80068-e7d1-448c-a2f6-4d4fbbb9bb48"><img border="none" src="https://my.setmore.com/webapp/images/bookappt/SetMore-book-button.png" alt="Book an appointment with Measure using SetMore" /></a>

<script>
function Quiz(questions) {
    this.score = 0;
    this.questions = questions;
    this.questionIndex = 0;
}

Quiz.prototype.getQuestionIndex = function() {
    return this.questions[this.questionIndex];
}

Quiz.prototype.guess = function(answer) {
    if(this.getQuestionIndex().isCorrectAnswer(answer)) {
        this.score++;
    }

    this.questionIndex++;
}

Quiz.prototype.isEnded = function() {
    return this.questionIndex === this.questions.length;
}


function Question(text, choices, answer) {
    this.text = text;
    this.choices = choices;
    this.answer = answer;
}

Question.prototype.isCorrectAnswer = function(choice) {
    return this.answer === choice;
}


function populate() {
    if(quiz.isEnded()) {
        showScores();
    }
    else {
        // show question
        var element = document.getElementById("question");
        element.innerHTML = quiz.getQuestionIndex().text;

        // show options
        var choices = quiz.getQuestionIndex().choices;
        for(var i = 0; i < choices.length; i++) {
            var element = document.getElementById("choice" + i);
            element.innerHTML = choices[i];
            guess("btn" + i, choices[i]);
        }

        showProgress();
    }
};

function guess(id, guess) {
    var button = document.getElementById(id);
    button.onclick = function() {
        quiz.guess(guess);
        populate();
    }
};


function showProgress() {
    var currentQuestionNumber = quiz.questionIndex + 1;
    var element = document.getElementById("progress");
    element.innerHTML = "Question " + currentQuestionNumber + " of " + quiz.questions.length;
};

function showScores() {
    var gameOverHTML = "<h1>Result</h1>";
    gameOverHTML += "<h2 id='score'> Your scores: " + quiz.score + "</h2>";
    gameOverHTML += "<button id='retake' class='butn btn-info' style='padding: 20px; width: auto;'>Take Again?</button>";
   // gameOverHTML += `<br/> <a id="Setmore_button_iframe" style="float:none" href="https://measure.setmore.com/bookingpage/7dd80068-e7d1-448c-a2f6-4d4fbbb9bb48"><img border="none" src="https://my.setmore.com/webapp/images/bookappt/SetMore-book-button.png" alt="Book an appointment with Measure using SetMore" /></a>`
    var element = document.getElementById("quiz");
    element.innerHTML = gameOverHTML;
};


// create questions here

var questions = [
new Question("What areas of your life do you need guidance with?",  ["Relationships", "Leadership", "Mental Health"], "Mental Health"),
new Question("What areas of menatl health do you need guidance with?", ["Dealing with negative emotions","Dealing with Anxiety", "Turning self-doubt into self-confidence"], "Dealing with negative emotions"),
new Question("After going through this program, what goals do you hope to achieve?", ["Stabalizing your thoughts", "Practice good coping skills", "Enhance self-confidence and self-love"], "Enhance self-confidence and self-love"),
new Question("Which gender would you prefer your mentor to be?", ["Male Mentor","Female Mentor","Non-binary Mentor"], "Female Mentor"),
new Question("How would you prefer to communicate with your mentor/mentee?", ["Email-Enter your email id?", "Phone-Enter your phone number?", "Skype-Enter your skype id?"], "Phone-Enter your phone number"),
];

// create quiz
var quiz = new Quiz(questions);

// display quiz
populate();

</script>





 --}}


